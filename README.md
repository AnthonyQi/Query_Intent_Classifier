# College Query Classifier

LoRA fine-tuned Qwen2.5 model that classifies college-related queries into:
- persona
- intent

## Files
- final_model.zip → trained LoRA adapter
- train_data.json → training dataset
- inference.py → run predictions
